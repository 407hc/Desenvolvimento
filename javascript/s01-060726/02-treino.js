//adicione um espaço na expressão salva em 'response' para fazer o código exibir 'sucess! tickets booked.' no console.
const feedback = "Success!";
const response = feedback + " Tickets booked.";
console.log(response);

//atualize a variável 'language' para "Spanish".
let language = "Spanish";

//encontre e exclua a palavra-chave 'let' extra no código.
let word1 = "a";
const word2 = word1 + "b";
word1 = word2 + "c";

//crie uma variável que possa ser alterada chamada status e use defaultStatus para definir seu valor, de modo que
//ambas as variáveis tenham o mesmo valor inicial.
const defaultStatus = "Hi there!";
let status = defaultStatus;

//crie uma variável que não mudará chamada 'buttonLabel' e defina-a com um valor para que o código exiba "Go" no console.
const startLabel = "Go";
const stopLabel = "Stop";
const buttonLabel = startLabel;
console.log(buttonLabel);

//escreva uma expressão usando essas variáveis e um espaço, "" para exibir 'Wonder Woman' no console.
const wonder = "Wonder";
const woman = "Woman";
const hero = wonder + " " + woman;
console.log(hero);

//reatribua a variável 'barista' para que 'Louise' cubra o turno de hoje.
let barista = "Brandon";
barista = "Louise";
console.log("Today's shift: " + barista);

//crie uma variável que não pode ser alterada chamada result e defina-a como uma expressão que soma 'word1' e 'word2'.
const word1 = "up";
const word2 = "date";
const result = word1 + word2;
